{
  "name": "aether-sdk",
  "version": "8.9.0",
  "private": true,
  "description": "Aether SDK \u2014 Multi-platform behavioral analytics and Web3 data collection",
  "workspaces": [
    "packages/shared",
    "packages/web",
    "packages/react-native",
    "frontend/aether",
    "frontend/kyber",
    "frontend/shared",
    "frontend/docs",
    "frontend/demo"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "tsc --noEmit --project packages/web/tsconfig.json && npm run typecheck --workspace=frontend/kyber --if-present && npm run typecheck --workspace=packages/react-native --if-present",
    "clean": "rm -rf packages/*/dist packages/*/node_modules",
    "docs": "typedoc --entryPoints packages/web/src/index.ts --out docs/api",
    "deps:circular": "python scripts/check_circular_deps.py packages/web/src packages/react-native/src frontend/kyber/src",
    "deps:unused": "npx --yes depcheck@^1.4 packages/web && npx --yes depcheck@^1.4 packages/react-native && npx --yes depcheck@^1.4 frontend/kyber",
    "security:secrets": "python scripts/security/secret_scan.py",
    "security:deps": "npm audit --omit=dev || true; (command -v pip-audit >/dev/null && pip-audit || echo 'pip-audit not installed (optional)')",
    "security:vuln": "echo 'Run a container/image scanner (e.g. trivy) against built images; see docs/VULNERABILITY-MANAGEMENT.md' ",
    "security:sbom": "npx --yes @cyclonedx/cyclonedx-npm@^1 --output-file sbom.json || echo 'SBOM tool unavailable; see docs/DEPENDENCY-AUDIT.md'",
    "security:licenses": "npx --yes license-checker@^25 --summary || echo 'license-checker unavailable; see docs/DEPENDENCY-AUDIT.md'",
    "security:audit": "npm run security:secrets && npm run security:deps",
    "compliance:readiness": "python scripts/compliance/readiness.py",
    "compliance:evidence": "python scripts/compliance/readiness.py --json",
    "api:openapi": "python scripts/export_openapi.py",
    "load:gen": "python tests/load/generate_synthetic.py",
    "load:smoke": "python tests/load/generate_synthetic.py --tenants 3 --events 100 >/dev/null && echo 'load smoke ok (synthetic generation)'",
    "load:ingestion": "locust -f tests/load/locustfile.py --host ${AETHER_HOST:-http://localhost:8000} || echo 'locust not installed; see docs/LOAD-TESTING.md'",
    "load:all": "npm run load:smoke"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "typedoc": "^0.25.0",
    "rollup": "^4.0.0",
    "@rollup/plugin-typescript": "^11.0.0",
    "@rollup/plugin-terser": "^0.4.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "tslib": "^2.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED"
}
