version: 1
frontend:
  buildPath: /
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build --workspace=frontend/status
  artifacts:
    baseDirectory: frontend/status/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
