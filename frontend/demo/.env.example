# Aether Demo App — closed, synthetic, fully-seeded demo environment.
# Default local-mocked: runs entirely on MSW fixtures, no backend required.
VITE_DEMO_ENV=local-mocked   # local-mocked | local-live | staging | production
VITE_API_BASE_URL=http://localhost:8000
