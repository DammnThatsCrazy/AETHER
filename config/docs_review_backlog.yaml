# Documented docs-review backlog — docs whose sources moved after their
# last GENUINE content review (mechanical last_synced_commit restamps do
# not count; scripts/docs_drift.py enforces that distinction).
#
# Contract (enforced by scripts/docs_drift.py --strict):
#   - a doc listed here may be stale without failing the gate;
#   - an UNLISTED stale doc fails the gate;
#   - a listed doc that is no longer stale FAILS the gate until its entry
#     is removed (shrink-only — the backlog cannot rot into a bypass);
#   - docs_drift --update refuses to stamp listed docs.
# Clearing an entry requires reviewing the doc against its source_files,
# updating content where contradicted, stamping, and deleting the entry.
#
# The AWS deployment and CI/CD pages were re-reviewed in the current delivery
# contract. Entries below record docs whose source_files moved at the #608
# main-merge squash (59a7fb24 — social360 / relationship-fidelity / risk360 /
# fraud360 / financial-normalization / spine-P0 convergences) WITHOUT a
# co-moving last_synced restamp from the owning program, so their review
# recency is unverifiable and they fail --strict. They are listed here so the
# gate is honest (stale is reported as advisory) until the owning program does
# a genuine content re-review and clears each entry.
docs:
  - path: docs/AGENTIC_COMMERCE_BUILD_SPEC.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning commerce lane merged without a last_synced restamp. Genuine content re-review pending with commerce@aether before stamp + entry removal.
    owner: commerce@aether
  - path: docs/COMMERCE-OPERATOR-RUNBOOK.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning commerce lane merged without a last_synced restamp. Genuine content re-review pending with commerce@aether before stamp + entry removal.
    owner: commerce@aether
  - path: docs/OPERATIONAL-INTELLIGENCE-AUDIT.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning intelligence/risk lane merged without a last_synced restamp. Genuine content re-review pending before stamp + entry removal.
    owner: platform@aether
  - path: docs/PROFILE-360-AGGREGATION.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning backend lane merged without a last_synced restamp. Genuine content re-review pending with backend@aether before stamp + entry removal.
    owner: backend@aether
  - path: docs/X402_AUDIT_REPORT.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning security lane merged without a last_synced restamp. Genuine content re-review pending with security@aether before stamp + entry removal.
    owner: security@aether
  - path: docs/frontend/PROFILE360_ARCHITECTURE.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning frontend lane merged without a last_synced restamp. Genuine content re-review pending with frontend@aether before stamp + entry removal.
    owner: frontend@aether
  - path: docs/productization/economic-interoperability-intelligence/DEPLOYMENT_PROFILE_MATRIX.md
    reason: source_files changed at main-merge 59a7fb24 (#608) and again by the sdk-universal-ingestion lane (new default-OFF ingestion replay/spine/consent flag blocks in config/settings.py + env examples). Economic-interop profile tables need a genuine re-review by the platform/economic maintainer before stamping.
    owner: platform@aether
  - path: docs/productization/economic-interoperability-intelligence/TARGET_ARCHITECTURE.md
    reason: source_files changed at main-merge 59a7fb24 (#608) and again by the sdk-universal-ingestion lane (new default-OFF ingestion replay/spine/consent flag blocks). Economic-interop architecture doc needs a genuine re-review by the platform/economic maintainer before stamping.
    owner: platform@aether
  - path: docs/productization/economic-interoperability-intelligence/GRAPH_PROJECTION_MODEL.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning platform lane merged without a last_synced restamp. Genuine content re-review pending with platform@aether before stamp + entry removal.
    owner: platform@aether
  - path: docs/productization/economic-interoperability-intelligence/OLYMPUS_INTELLIGENCE_SURFACES.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning platform lane merged without a last_synced restamp. Genuine content re-review pending with platform@aether before stamp + entry removal.
    owner: platform@aether
  - path: docs/runbooks/CARD_LINKED_RUNBOOK.md
    reason: source_files changed at main-merge 59a7fb24 (#608); owning platform lane merged without a last_synced restamp. Genuine content re-review pending with platform@aether before stamp + entry removal.
    owner: platform@aether
