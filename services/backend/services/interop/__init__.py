"""Interoperability Intelligence — protocol-neutral observation-only domain.

Canonical cross-network message lifecycle, correlation, security-policy
snapshots, and a provider adapter framework with LayerZero V2 as the
fixture-proven reference adapter. AETHER OBSERVES. AETHER DOES NOT RELAY:
no code path sends, routes, retries, or recovers a message, and every
tenant-scoped record carries execution_by_aether=False.
"""
