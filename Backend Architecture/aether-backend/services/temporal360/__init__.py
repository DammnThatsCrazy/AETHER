"""temporal360 — the contextual time projection (context-360 program, Phase 2).

Converges the ``temporal360`` registry row to ``implemented`` by resolving its
pending authority ``graph_history_replay`` (T2.1) and then serving the
projection surface (T2.2/T2.3). A 360 is an intelligence projection over
canonical Aether truth — never a competing system of record (ADR-010).
"""
